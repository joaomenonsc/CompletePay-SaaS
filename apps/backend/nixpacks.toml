[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.setup]
nixPkgs = ["python312", "gcc", "postgresql"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install ."
]

[start]
cmd = "python -m uvicorn src.api.app:app --host 0.0.0.0 --port ${PORT:-8000}"
