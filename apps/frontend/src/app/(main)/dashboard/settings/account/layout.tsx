import { SettingsAccountLayout } from "../_components/settings-account-layout";

export default function AccountLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
